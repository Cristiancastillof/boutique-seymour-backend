PORT=3000
NODE_ENV=development

# Desarrollo local
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=boutique_seymour
DB_PORT=5432

# Producción: pega aquí la connection string de Neon.
# Cuando DATABASE_URL existe, el backend la usa en lugar de las variables DB_*.
DATABASE_URL=

JWT_SECRET=cambia_esta_clave_por_una_larga_y_segura

# Se pueden permitir varios orígenes separados por coma.
CORS_ORIGIN=http://localhost:5173
